const inputExists = (args) => {
    if (args.length < 3) {
        throw new Error(`Missing arguments`);
    }


//   if (process.argv.length === 2) {
//     errMsg=`Incorrect input. Expecting encryption offset number.`
//   }
    return;
}
